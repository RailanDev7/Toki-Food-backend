
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model teste{ 
  id Int @id @default(autoincrement())
  sla String
  teste32 String
}